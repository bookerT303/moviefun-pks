---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: moviefun-claim
  annotations:
    volume.beta.kubernetes.io/storage-class: thin
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

